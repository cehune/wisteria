name: unit tests

on:
pull_request:
branches: [main]

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run CPU tests
        run: |
          xcodebuild test \
            -scheme "tests" \
            -destination "platform=macOS"